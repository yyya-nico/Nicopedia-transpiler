---
interface Props {
	data: [
        {
            id: string;
            title: string;
        }
    ];
    width: number;
    height: number;
}

const { data, width, height } = Astro.props;
---
<div class="wrapper">
    <div class="box" style={{ width: `${width + (10 + 1) * 2}px`, height: `${height + (10 + 1) * 2}px` }}>
        {data.map(entry => {
            return <div>
                    <a name={entry.id}></a>
                    <a class="oekaki-link" href={`/oekaki_id/${entry.id}`}>
                    <img src={`/oekaki/${entry.id}.png`} alt={entry.title} title={entry.title} width={width} height={height} />
                </a>
            </div>
        })}
    </div>
    <div class="tabs" style={{ width: `${width + (10 + 1) * 2}px` }}>
        {data.map(entry => {
            return <a class="tab" href={`#${entry.id}`}>{entry.title}</a>
        })}
        <div class="clear"></div>
    </div>
</div>
<style>
.wrapper {
    margin: 10px;
    overflow: auto;
}
.box {
    overflow: hidden;
    border: 1px solid #111;
}
.oekaki-link {
    margin: 10px;
}
.tab {
    margin-top: -1px;
    padding: 5px 10px;
    background-color: #fff;
    border: 1px solid #111;
    border-top: none;
    margin-right: 5px;
    text-decoration: none;
    color: #333;
    float: left;
}
.clear {
    clear: both;
}
a {
    display: block;
}
img {
    vertical-align: middle;
}
</style>